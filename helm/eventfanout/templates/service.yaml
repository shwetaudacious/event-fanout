apiVersion: v1
kind: Service
metadata:
  name: {{ include "event-fanout.fullname" . }}
  labels:
    {{- include "event-fanout.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "event-fanout.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: server
