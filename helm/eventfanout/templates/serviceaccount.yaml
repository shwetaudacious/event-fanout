apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "event-fanout.serviceAccountName" . }}
  labels:
    {{- include "event-fanout.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
